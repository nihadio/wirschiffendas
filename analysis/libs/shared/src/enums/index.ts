export enum Cluster {
  FLUIDS = "fluids",
  DRIVETRAIN = "drivetrain",
  MECHANICAL = "mechanical",
  EMS = "ems",
}

export enum AlgorithmResult {
  RUNNING = "running",
  READY = "ready",
  FAILED = "failed",
}

export enum AnalysisResult {
  OK = "ok",
  FAILED = "failed",
}
